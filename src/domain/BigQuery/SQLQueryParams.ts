/**
 * @typedef {Object} SQLQueryParams
 * @property {string} q SQL string
 * Contains all the parameters needed to send a query to the query endpoint
 */
export type SQLQueryParams = {
  q: string;
};
